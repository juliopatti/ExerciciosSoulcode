#2) Faça um Programa que leia uma lista de 10 números reais e mostre-os na ordem inversa.

lista=[]
for i in range(0,10):
    while True:
        try:
            lista.append(float(input(f"Entre com o {i+1}º nº real: ")))
            break
        except:
            print("Entrada Inválida! Entre com um nº real:")
lista.reverse()
print(f"\nA lista dos nºs digitados, em ordem inversa, é:\n {lista}")
